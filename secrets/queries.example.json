{
      "Moodle courses": "SELECT c.id, c.category, c.fullname, c.shortname, c.visible, cc.name AS category_name, CONCAT( RIGHT( cc.path, (LENGTH( cc.path ) -1 ) ) , \"/\", c.id) AS arbre FROM `mdl_course` AS c LEFT JOIN `mdl_course_categories` AS cc ON c.category = cc.id",
      "_Gaaps": "This query will not be executed as key starts with: _  "
}
